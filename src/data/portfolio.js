// Este arquivo agora é só um re-export. Os dados foram quebrados por seção
// dentro de `./portfolio/` — edite lá. Veja `./portfolio/index.js` para o mapa
// de onde fica cada coisa.
export { languageOptions, contentByLanguage } from './portfolio/index.js'
